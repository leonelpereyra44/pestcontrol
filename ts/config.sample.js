// Copia este archivo a ts/config.js y completa con tus valores locales.
// En producción (Vercel), se generará automáticamente desde variables de entorno
// mediante el script scripts/generate-config.mjs.

window.APP_CONFIG = {
  // URL del proyecto Supabase (ej: https://xxxx.supabase.co)
  SUPABASE_URL: "",
  // Clave pública anon (NO service role)
  SUPABASE_ANON_KEY: ""
};
